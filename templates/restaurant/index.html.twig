{% extends 'base.html.twig' %}

{% block title %}Restaurant index{% endblock %}
{% block stylesheets %}{% endblock %}


{% block body %}
<div class="row d-flex justify-content-center text-center">
    {% for restaurant in restaurants %}
            <div class="card m-1" style="width: 18rem;">
                <img src="{{restaurant.photo}}" class="card-img-top" alt="photo de {{ restaurant.nom }}">
                    <div class="card-body">
                        <h5 class="card-title">{{ restaurant.nom }}</h5>
                        <a href="{{ path('restaurant_show', {'id': restaurant.id}) }}">Voir le restaurant</a>
                        {% if is_granted("ROLE_ADMIN") %}
                        <a href="{{ path('admin_restaurant_edit', {'id': restaurant.id}) }}">Modifier</a>
                        {% endif %}
                    </div>
            </div>
    {% else %}
    <p> Pas de restaurants sur le site </p>
    {% endfor %}
</div>
     
  <div class="row">
    <div class="col text-center pb-5 pt-5">
        <a href="{{path('home2')}}">
            <button class="btn btn-secondary" type="submit">
            Retour à l'accueil
            </button>
        </a>
    </div>
    <div class="col text-center pb-5 pt-5">
        <a href="{{ path('restaurant_new') }}">
            <button class="btn btn-secondary" type="submit">
            Proposer un restaurant
            </button>
        </a>
    </div>
</div>
 
{% endblock %}
