{% extends 'base.html.twig' %}

{% block title %}OAAF{% endblock %}

{% block body %}

  
<!--DESCRIPTION DU PROJET-->
<div class="container">
    <div class="col justify-content-center pb-5">
      <h4>Nous avons décidé de faire un site pour référencer des restaurants à Paris, avec une cuisine variée et éclectique. Que vous vouliez profiter d’un lieu avec des plats originaux et typiques d'une région, ou que vous soyez en quête de nouveautés, nous espérons que vous trouverez votre bonheur ici. Nous avons également intégré des catégories qui indiquent si le restaurant dispose de menus vegan, végétarien ou sans gluten. Nous vous souhaitons une bonne visite.
      </h4>
      
    </div>
  <!--CATEGORIES DE RESTAURANTS-->
  <div class="container overflow-hidden">
    {% for categorie in categories %}
      <div class="row"><div class="col text-center pb-3 pt-3"><h1>{{ categorie.cat }}</h1></div></div>
      <div class="row"><div class="col text-center pb-3 pt-3"><img src="{{ categorie.photo }}" alt="photo de {{ categorie.cat }}"></div></div>
      <div class="row"><div class="col text-center pb-3 pt-3"><h4 class="h3 mb-3 font-weight-normal">{{ categorie.description }}</h4></div></div>
      <div class="row"><div class="col text-center pb-5 pt-3"><a class="btn btn-secondary btn-md" href="{{ path('restaurant_par_cat', {'cat': categorie.id}) }}">Voir la catégorie</a></div></div>
    {% endfor %}
    </div>

    {% if is_granted("IS_AUTHENTICATED_FULLY") %}
    <div class="col text-center pb-5 pt-5">
        <a href="{{ path('restaurant_new') }}">
            <button class="btn btn-secondary" type="submit">
            Proposer un restaurant
            </button>
        </a>
    </div>
    {% endif %}
</div>
</div>


{% endblock %}
